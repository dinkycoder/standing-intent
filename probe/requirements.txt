# Week-1 probe dependencies only. NOT the project's dependency set.
#
# The x402 SDK source is the x402-foundation reference clone at
#   C:\Users\dinky\projects\x402-reference   ->  python/x402
#   repo commit e398a9e (2026-08-28), package version: x402 2.21.0
#
# The "x402" name is polluted on PyPI (TRON forks, Solana-only ports, unrelated
# commercial products). Install from the verified source. Pin the exact version
# into the main project only after probe_02 has actually settled on chain.

requests
python-dotenv
eth-account

# x402 v2 SDK: sync "requests" client + EVM ("exact" scheme, Base / Base Sepolia).
#
# Preferred — install from the local verified clone:
#   python -m pip install -e "C:\Users\dinky\projects\x402-reference\python\x402[requests,evm]"
#
# Or, once the PyPI package is confirmed to be the x402-foundation one:
x402[requests,evm]
